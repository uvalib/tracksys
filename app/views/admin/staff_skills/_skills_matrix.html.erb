<div class="skills-matrix-wrapper">
   <div class="skills-matrix">
      <div class="skills-panel">
            <label class="header">Category</label>
            <div class="category-selector">
               <select id="category">
                  <option>Select a category</option>
                  <% Category.all.each do |c| %>
                     <option val="<%= c.id %>"><%= c.name %></option>
                  <% end %>
               </select>
            </div>
         <div class="scroller">
            <div class="staff-skills">
            </div>
         </div>
      </div>
      <div class="skill-buttons-panel">
         <span class="mf-action-button"> << </span>
         <span class="mf-action-button"> >> </span>
      </div>
      <div class="staff-panel">
         <label class="header">Staff Members</label>
         <div class="scroller">
            <div class="staff-list">
               <% StaffMember.all.order(last_name: :asc).each do |s| %>
                  <p class="staff"><%= s.full_name %></p>
               <% end %>
            </div>
         </div>
      </div>
   </div>
</div>
